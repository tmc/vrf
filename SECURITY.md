# Security Policy

Package `github.com/tmc/vrf` has not received an independent security audit.

For `v0.1.0`, the public package implements the Algorand-compatible draft-03
VRF suite only. The RFC 9381 work area remains under `internal/rfc9381` and is
not public API.

Report suspected vulnerabilities privately to Travis Cline.
